# Automated Followspot System - Top-level requirements
# Install specific stack requirements:
#   pip install -r control/requirements.txt   # For control stack
#   pip install -r node/requirements.txt      # For node stack (Pi)
#   pip install -r reid/requirements.txt      # For ReID stack
#   pip install -r tools/requirements.txt     # For tools

# Core (shared)
numpy>=2.5.1
opencv-python-headless>=5.0.0.93
aiohttp>=3.14.2
aiortc>=1.15.0
pillow>=12.3.0
requests>=2.34.2
pyserial>=3.5
scipy>=1.18.0
scikit-learn>=1.9.0

# Control stack
torch>=2.13.0
torchvision>=0.28.0
ultralytics>=8.4.102
torchreid>=0.2.5
ultralytics>=8.4.102

# Node stack
picamera2>=0.3.36
scikit-image>=0.26.0
Flask>=3.1.3

# ReID stack
# (uses control stack deps)

# Tools
# coremltools>=6.4  # Optional, for Apple Silicon model conversion