#pragma once

// Default Wi-Fi credentials compiled into firmware.
// The Python flasher script will copy this template to wifi_credentials.h
// and fill in your SSID/password.
#define WIFI_SSID_DEFAULT "YourSSID"
#define WIFI_PASS_DEFAULT "YourPassword"
#define BEACON_NAME_DEFAULT "Beacon"
